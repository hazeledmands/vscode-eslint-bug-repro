// there should be an eslint error on the next line because there's no semicolon
import {format} from 'util'

// there should be a typescript error on the next line because format() doesnt' have args
console.log(format()); 